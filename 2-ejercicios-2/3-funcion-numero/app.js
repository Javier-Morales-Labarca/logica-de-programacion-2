//Crear una función que reciba un número como parámetro y devuelva el doble de ese número.

function agregar(numero) {
    console.log(`El doble del número ingresado es: ${numero * 2}`);
    }

let numero = prompt("Ingrese un número: ");

agregar(numero);